import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const CustomMessage = () => {
  return (
    <View>
      <Text>Message In Progress</Text>
    </View>
  );
};

export default CustomMessage;

const styles = StyleSheet.create({});
