import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const PendingOrders = () => {
	return (
		<View>
			<Text>Soemthing about pending orders</Text>
		</View>
	);
};

export default PendingOrders;

const styles = StyleSheet.create({});
